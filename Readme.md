# Pending Features
    1> .Select() we have to set at the end of the project
    2> We also also have to set the rate limit 
    3> MongoDB Indexing we have to set it 
    4> We have to find the origin setting at the index for the socket.io